<UserControl x:Class="DCSB.Views.BindKeysView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:DCSB.Views"
             mc:Ignorable="d" 
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{StaticResource MaterialDesignFont}"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}">

    <materialDesign:Card Padding="5">
        <StackPanel>
            <TextBlock
                Margin="5"
                TextAlignment="Center"
                FontWeight="Medium"
                FontSize="14">Press keys you want to bind.</TextBlock>
            <StackPanel Orientation="Horizontal"
                        FlowDirection="RightToLeft">
                <Button Margin="5" Command="{Binding ClearKeysCommand}">Clear</Button>
            </StackPanel>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
